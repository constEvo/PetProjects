#ifndef ESTATES_H
#define ESTATES_H

enum EDirection {Idle, Up, Down, Left, Right};

enum EWeaponState {SingleShotWeapon, TripleShotWeapon };

enum EGameState {InGame, InMenu, GameOver };



#endif // ESTATES_H
